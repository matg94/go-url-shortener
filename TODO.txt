* Change repo store url to take a models.url object rather than fields
* Check for invalid requests
* Add config to switch between real redis and cache
* Add url_service get hits test
* Add custom errors at each level and handle them individually
* Controller tests
* models tests
Setup proper logging
Setup CI/CD
Add linting
Setup docker
Add Terraform deployments